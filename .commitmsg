feat(templates): add allauth templates and Bootstrap base layout

- Copy all allauth account templates to templates/allauth/account/
  (login, signup, logout, password reset, email management, etc.)
- Copy all allauth socialaccount templates to templates/allauth/socialaccount/
  (connections, login, signup, error handling, snippets)
- Add templates/base.html with auth-aware navigation menu
- Add templates/allauth/base.html with Bootstrap 4.6 CDN
- Update TEMPLATES DIRS in settings.py to discover custom templates
- Add Development Diary Entry 8
